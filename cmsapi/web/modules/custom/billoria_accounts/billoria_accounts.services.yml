services:
  logger.channel.billoria_accounts:
    parent: logger.channel_base
    arguments: ['billoria_accounts']

  billoria_accounts.user_verification:
    class: Drupal\billoria_accounts\Service\UserVerificationService
    arguments: ['@database', '@current_user', '@logger.channel.billoria_accounts']
